# Визуальный профиль университета

LOCUS Startup Hackathon 2026 — Кейс 01 (`LOCUSCASE1`)

AI-сервис: по названию университета формирует проверенный визуальный профиль
кампуса — фото по категориям, без дублей, с указанием источника и честной
оценкой достоверности.

## Задача

Абитуриент видит красивые рекламные страницы, но достоверные фото кампуса
разбросаны по десяткам источников. Сервис принимает название университета,
находит изображения, проверяет их принадлежность, удаляет дубликаты и
формирует профиль со ссылками на источники.

## Стек

- **Frontend**: Next.js 16 (App Router) + React 19 + Tailwind CSS 4, деплой на Vercel
- **Backend**: Next.js Route Handlers (`/api/search`), serverless
- **Поиск изображений**: Wikimedia Commons + Openverse (без ключей, фото несут
  источник/лицензию/дату) + официальный сайт университета (фолбэк для охвата
  вузов без крупного crowd-sourced присутствия)
- **Дедупликация**: perceptual average-hash (8×8 grayscale, `sharp`) + Hamming distance
- **Верификация принадлежности / категоризация**: Google Gemini Vision (бесплатный ключ, без карты)
- **Описание кампуса**: Gemini (текст) на основе Wikidata/Wikipedia
- **Разрешение запросов**: Wikidata Search API как основной путь (см. ниже),
  Wikipedia full-text search как резерв

## Архитектура пайплайна

1. `resolveUniversity` — резолвинг названия **в первую очередь через
   Wikidata** (`wbsearchentities`): Wikidata индексирует значительно больше
   из ~25 000 университетов мира, чем есть отдельных статей в English
   Wikipedia — у большинства вузов есть хотя бы краткая карточка с
   названием/описанием/сайтом, даже без статьи. Из найденной карточки
   вытягиваются город (P131), страна (P17) и **официальный сайт (P856)**
   одним запросом; если есть привязанная статья в English Wikipedia —
   подтягивается её краткое описание. Если Wikidata ничего не находит —
   резерв: полнотекстовый поиск по Wikipedia с проверкой смыслового
   пересечения с запросом (чтобы не подставить случайную непричастную
   статью вместо честного "не найдено").
2. `searchAllCategories` — широкий пул изображений: Wikimedia Commons +
   Openverse (базовый поиск по названию университета + поиск по городу +
   точечные запросы-подсказки по категориям) **плюс** парсинг фотографий с
   официального сайта вуза (главная страница, найдена через P856) — это и
   есть основной рычаг масштабирования на весь длинный хвост университетов,
   у которых почти нет фото в открытых crowd-sourced источниках.
3. `dedupeCandidates` — вычисление perceptual hash для каждого изображения,
   отсев дублей и визуально похожих кадров (Hamming distance ≤ 6/64).
4. `verifyBatch` — Gemini Vision смотрит на каждое оставшееся изображение и
   (а) подтверждает принадлежность университету/городу, (б) сам определяет
   итоговую категорию из содержимого фото (не из поискового хинта или
   источника), (в) возвращает `confidence` (0–1) и текстовое пояснение на
   русском.
5. Изображения с `confidence < 0.35` скрываются, `0.35–0.6` помечаются как
   низкая достоверность (жёлтый бейдж) — реализация принципа "честная
   неопределённость" из требований кейса. Для вузов без Wikidata-карточки и
   без фото сервис явно сообщает об этом вместо того, чтобы что-то выдумать.
6. `generateDescription` — краткое факт-ориентированное описание на основе
   найденного текстового источника (без домысливания).

## Запуск локально

```bash
npm install
cp .env.local.example .env.local   # заполнить ключи
npm run dev
```

Откроется `http://localhost:3000`.

## Переменные окружения

См. `.env.local.example`:

- `GEMINI_API_KEY`, `GEMINI_VISION_MODEL`, `GEMINI_TEXT_MODEL` — Google Gemini
  (aistudio.google.com/apikey, бесплатно, без карты)
- Wikimedia Commons и Openverse ключей не требуют.

## Тестовый сценарий

1. Ввести название университета (например, не из демо-видео).
2. Дождаться результата (~10–25 сек в зависимости от числа найденных фото).
3. Проверить: категории заполнены, под каждым фото — % достоверности и
   кликабельный источник, дубликаты отсутствуют, при нехватке данных —
   явное предупреждение сверху.
4. Использовать фильтры по категориям.

## Ограничения

- Дата публикации доступна для фото с Wikimedia Commons (EXIF/метаданные
  загрузки); Openverse дату не всегда предоставляет.
- Скорость поиска зависит от числа найденных кандидатов и задержек внешних
  API (Commons/Openverse/Wikipedia/Gemini).
- Для университетов без карточки в Wikidata и без официального сайта в
  открытом доступе сервис честно возвращает пустой профиль с пояснением,
  а не выдуманные данные.
- Парсинг официального сайта смотрит только на главную страницу (структура
  сайтов слишком разная для надёжного глубокого обхода в рамках 30 сек).

## AI/API

- Wikidata Search/Entity API — резолвинг университета, город/страна/сайт.
- Wikimedia Commons API + Openverse API — поиск изображений (без ключей).
- Официальный сайт университета — дополнительный источник фото (fallback).
- Google Gemini API (vision + text) — верификация фото и генерация описания.
- Wikipedia REST API — краткое текстовое описание (когда есть статья).

## Роли команды

_Заполнить перед сабмитом._
